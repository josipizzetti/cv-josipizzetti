
  # Single Page Portfolio Website

  This is a code bundle for Single Page Portfolio Website. The original project is available at Figma.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.

  ## TODO:

  - [ ] extract components
  - [ ] printable version - pdf
  